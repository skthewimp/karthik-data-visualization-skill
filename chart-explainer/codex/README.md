# chart-explainer / codex

Codex-ready version of the `chart-explainer` skill.

Install target: `~/.codex/skills/chart-explainer/`

Edit this when changing how Codex should narrate a finished chart or table for a reader who was not in the analysis. `examples.md` ships alongside `SKILL.md` because the skill loads it at runtime for calibration.
