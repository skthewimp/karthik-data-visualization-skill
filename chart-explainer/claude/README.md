# chart-explainer / claude

Claude-ready version of the `chart-explainer` skill.

Install target: `~/.claude/skills/chart-explainer/`

This mirrors the Codex version, but keeps Claude-compatible frontmatter and shorter description text. `examples.md` ships alongside `SKILL.md` because the skill loads it at runtime for calibration.
