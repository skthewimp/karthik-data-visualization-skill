# dataviz-selector / claude

Claude-ready version of the `dataviz-selector` skill.

Install target: `~/.claude/skills/dataviz-selector/`

This usually mirrors the Codex version, but keeps Claude-compatible frontmatter and shorter description text.
