# dataviz-selector scripts

Local helper scripts for developing or testing the `dataviz-selector` skill.

Scripts in this folder are ignored by default. If a script becomes part of the public workflow, document what it does, how to run it, and what inputs/outputs it expects before unignoring it.
