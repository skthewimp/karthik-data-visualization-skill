# karthik-r-analysis-style / claude

Claude-ready version of the `karthik-r-analysis-style` skill.

Install target: `~/.claude/skills/karthik-r-analysis-style/`

This mirrors the Codex version, but keeps Claude-compatible frontmatter and shorter description text. The `references/` folder ships inside this directory because the skill reads those files at runtime.
