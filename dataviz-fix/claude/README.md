# dataviz-fix / claude

Claude- and Hermes-compatible version of the iterative chart-repair and skill-learning workflow.

Install targets:

- `~/.claude/skills/dataviz-fix/`
- `~/.hermes/skills/data-science/dataviz-fix/` through `./sync.sh --no-pull --surface hermes`

Keep behavioural changes aligned with `../codex/SKILL.md`. Keep `scripts/case_manager.py` byte-identical across both surfaces.
