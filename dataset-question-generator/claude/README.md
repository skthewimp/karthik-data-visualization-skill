# dataset-question-generator / claude

Claude-ready version of the `dataset-question-generator` skill.

Install target: `~/.claude/skills/dataset-question-generator/`

This usually mirrors the Codex version, but keeps Claude-compatible frontmatter and a shorter trigger description. Edit this with `../codex/SKILL.md` when the question-generation workflow changes.
