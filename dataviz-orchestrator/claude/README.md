# dataviz-orchestrator / claude

Claude-ready version of the `dataviz-orchestrator` skill.

Install target: `~/.claude/skills/dataviz-orchestrator/`

This usually mirrors the Codex version, but keeps Claude-compatible frontmatter and shorter trigger text. Edit this with `../codex/SKILL.md` when the orchestration workflow changes.
