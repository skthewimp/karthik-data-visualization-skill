# dataviz-idea-critique / codex

Codex version of the pre-render idea gate: is the data right, the expression right, the insight right, and honest - before the chart is built.

Install target: `~/.codex/skills/dataviz-idea-critique/`.

Keep behavioural changes aligned with `../claude/SKILL.md` (the two SKILL.md files are byte-identical). This is distinct from `dataviz-critique`, which reviews a rendered chart.
