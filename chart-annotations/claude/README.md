# chart-annotations / claude

Claude-ready version of the `chart-annotations` skill.

Install target: `~/.claude/skills/chart-annotations/`

This mirrors the Codex version, but keeps Claude-compatible frontmatter and shorter description text.
