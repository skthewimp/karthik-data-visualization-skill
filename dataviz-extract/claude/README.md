# dataviz-extract / claude

Claude-ready version of the `dataviz-extract` skill.

Install target: `~/.claude/skills/dataviz-extract/`

Use this surface when editing how Claude reads the full period-by-category data table out of a chart image. Keep `SKILL.md` byte-identical to `../codex/SKILL.md`.
